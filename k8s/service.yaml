apiVersion: v1
kind: Service
metadata:
  name: portfolio
spec:
  selector:
    app: portfolio
  ports:
    - port: 80
      targetPort: http
      name: http
  type: ClusterIP # front this with an Ingress (or a cloud LoadBalancer Service) rather than exposing it directly
